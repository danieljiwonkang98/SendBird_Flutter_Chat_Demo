import 'package:app/themes/colors.dart';
import 'package:flutter/material.dart';

final theme = ThemeData(
  primaryColor: Colors.deepPurple,
  appBarTheme: const AppBarTheme(color: ThemeColors.primary),
);
